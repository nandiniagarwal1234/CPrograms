/*************************
WAPC to accept the weight of a parcel in kilograms and calculate the rate per kilogram based on the following criteria:
Weight in kilograms
Rate per kilogram
First 5 kilograms
Rs. 800
Next 5 kilograms
Rs. 700
Above 10 kilograms
Rs. 500
Also, input the type of the courier (‘I’ for International and ‘D’ for Domestic). If the type of courier is International, an additional amount of Rs. 1500 is levied.
*******************/
#include <stdio.h>

int main() {
    float weight, cost = 0.0;
    char type;

    // Input weight and courier type
    printf("Enter the weight of the parcel (in kg): ");
    scanf("%f", &weight);

    printf("Enter courier type ('I' for International, 'D' for Domestic): ");
    scanf(" %c", &type); // Note the space before %c to consume any leftover newline

    // Calculate cost based on weight slabs
    if (weight <= 5) {
        cost = weight * 800;
    } else if (weight <= 10) {
        cost = (5 * 800) + ((weight - 5) * 700);
    } else {
        cost = (5 * 800) + (5 * 700) + ((weight - 10) * 500);
    }

    // Add surcharge for international courier
    if (type == 'I' || type == 'i') {
        cost += 1500;
    }

    // Display the total cost
    printf("Total courier cost = Rs. %.2f\n", cost);

    return 0;
}
