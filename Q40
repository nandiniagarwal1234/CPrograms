//WAPC to input a positive integer. Check whether it is Kaprekar or not. Kaprekar Number – A number whose square can be split into two parts that add up to the number itself. Example: 45² = 2025 → 20 + 25 = 45
#include <stdio.h>
#include <math.h>

int main() {
    int num, sq, digits, divisor, left, right;

    printf("Enter a positive integer: ");
    scanf("%d", &num);

    sq = num * num;

    // Count digits in the original number
    digits = (num == 0) ? 1 : log10(num) + 1;

    // Create divisor to split the square
    divisor = pow(10, digits);

    left = sq / divisor;
    right = sq % divisor;

    if(left + right == num)
        printf("%d is a Kaprekar number.\n", num);
    else
        printf("%d is not a Kaprekar number.\n", num);

    return 0;
}
